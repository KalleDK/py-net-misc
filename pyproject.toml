[project]
name = "net-misc"
version = "0.1.5.1"
description = "Add misc utils for networking calculations"
readme = "README.md"
requires-python = ">=3.13"
authors = [
    {name = "Kalle M. Krog Aagaard", email = "git@k-moeller.dk"},
]
maintainers = [
    {name = "Kalle M. Krog Aagaard", email = "git@k-moeller.dk"},
]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
]

[project.urls]
Homepage = "https://github.com/KalleDK/py-net-misc"
Documentation = "https://github.com/KalleDK/py-net-misc"
Repository = "https://github.com/KalleDK/py-net-misc.git"

[project.optional-dependencies]
pydantic = [
    "pydantic-core>=2.48.0",
]

[dependency-groups]
test = [
    "coverage>=7.15.4",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]
stubs = [
    "pydantic>=2.14.0b1",
]

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "net_misc"

[tool.pyright]
include = ["src", "tests", "examples"]
typeCheckingMode = "strict"
pythonVersion = "3.13"

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.pytest.ini_options]
addopts = "-v --cov=net_misc --cov-report=term-missing --cov-report=xml"
testpaths = ["tests"]
